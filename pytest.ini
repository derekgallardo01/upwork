[pytest]
# standalone/ holds publish-ready copies of the assets (their tests run inside
# their own repos). Exclude them here so root collection doesn't hit duplicate
# test-module names.
norecursedirs = standalone .git
